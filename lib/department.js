const Employee = require("./Employee")

class Department extends Employee {
    constructor (firstname, lastname, roleid, managerid, github){
        super (name, employeeId, email, position)
        this.github = github;
    }
}